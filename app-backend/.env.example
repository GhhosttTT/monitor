# 服务器配置
PORT=5002
NODE_ENV=production

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=monitor_system
DB_USER=root
DB_PASSWORD=your_password_here

# Redis配置（如果使用）
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS配置（允许的前端域名）
CORS_ORIGIN=*
